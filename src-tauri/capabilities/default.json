{
  "$schema": "https://schema.tauri.app/config/2/capability.json",
  "identifier": "default",
  "description": "Default window capability set for Phase 1",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
